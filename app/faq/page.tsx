"use client";
import { useState } from "react";
import { ChevronDown } from "lucide-react";
import { PublicPage } from "@/components/PublicPage";
const items = [["How do I earn XP and rank up?", "Daily Challenges award 50 XP, completed modules award 25 XP, and Practice Labs award configured rewards commonly ranging from 50 to 150 XP."], ["When does the Daily Challenge reset?", "The Daily Challenge resets every 24 hours. Submissions are locked after one submission until the next challenge window."], ["How are CTF flags validated?", "Flags are trimmed and normalized to uppercase before comparison, so letter case does not cause a valid answer to fail."], ["Are labs run in isolated sandbox environments?", "Yes. ByteBreach labs are simulated defensive and offensive testbeds intended for authorized learning and research."]];
export default function FaqPage() { const [open, setOpen] = useState(0); return <PublicPage eyebrow="Knowledge base" title="Frequently Asked Questions"><div className="space-y-3">{items.map(([question, answer], index) => <div key={question} className="rounded-xl border border-line"><button onClick={() => setOpen(open === index ? -1 : index)} className="flex w-full items-center justify-between p-4 text-left text-sm font-semibold text-slate-100">{question}<ChevronDown className={`text-cyan transition ${open === index ? "rotate-180" : ""}`} size={17}/></button>{open === index && <p className="border-t border-line p-4 text-sm text-muted">{answer}</p>}</div>)}</div></PublicPage>; }
